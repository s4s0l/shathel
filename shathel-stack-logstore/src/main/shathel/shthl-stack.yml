version: 1
shathel-stack:
  deployName: logstore

  pre-provisioners:
    configure:
      inline: api.setKernelParam('vm.max_map_count=262144')
  enrichers:
      addNetwork:
        target: DEPS
        inline: >
          compose.addExternalNetworkAndAttachAllServices("logstore_network")