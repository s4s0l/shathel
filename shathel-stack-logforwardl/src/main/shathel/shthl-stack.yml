version: 1
shathel-stack:
  deployName: logforwardl

  enrichers:
      logDriver:
        target: ALLOTHERS
        inline: >
          compose.yml.services.each { it->
            it.value.logging = [
                       driver: 'gelf',
                       options: [
                          'gelf-address':"udp://${env.getIpForManagementNode()}:12201".toString(),
                          tag:stack.name
                       ]

                    ]
          }
          []
  dependencies:
      org.s4s0l.shathel:shathel-stack-logstore:$version:

