language: java
jdk:
  - oraclejdk8
env:
  - DOCKER_VERSION=1.12.6 COMPOSE_VERSION=1.9.0


build:

  ci:
    - docker build -t dind4java:1.13.0 sandbox/did4java
    - docker run --privileged --name some-docker -v /root/src/github.com/s4s0l/shathel:/build --rm -ti dind4java:1.13.0 ls -al /build
