version: "3.1"
services:
  service:
    image: tutum/hello-world
    environment:
      - DEPENDENCY2=${DEPENDENCY2:-absent}
      - DEPENDENCY3=${DEPENDENCY3:-absent}
      - DEPENDENCY4=${DEPENDENCY4:-absent}