version: "3.1"
services:
  service:
    image: tutum/hello-world
    secrets:
      - shathel_some_pass
      - donottouch


secrets:
  shathel_some_pass:
    file: ./secret/initialSecret
  donottouch:
    file: ./secret/initialSecret2
